{
  "name": "video-chat",
  "version": "1.0.0",
  "description": "A simple WebRTC video chat app (React + Node)",
  "scripts": {
    "postinstall": "concurrently 'npm run install:frontend' 'npm run install:backend'",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && npm install",
    "image": "concurrently 'npm run image:frontend' 'npm run image:backend'",
    "image:frontend": "cd frontend && npm run build && docker build -t ${IMAGE_ID:-janole/video-chat}/frontend:${IMAGE_VER:-dev} .",
    "image:backend": "cd backend && docker build -t ${IMAGE_ID:-janole/video-chat}/backend:${IMAGE_VER:-dev} .",
    "dev": "concurrently 'cd backend && npm run dev' 'cd frontend && npm start'"
  },
  "author": "Jan Ole Suhr",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.2.0"
  }
}