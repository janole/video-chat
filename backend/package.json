{
  "name": "server",
  "version": "0.1.0",
  "scripts": {
    "start": "node server",
    "dev": "nodemon server",
    "image": "docker build -t docker.janole.com/videochat-backend:$npm_package_version ."
  },
  "dependencies": {
    "express": "^4.16.4",
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
